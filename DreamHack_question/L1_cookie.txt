cookie 값을 변조해서 flag를 얻는 문제인가보다

코드를 다운 받아서 분석해 보았다. 

유저는 guest, admin이 있다

id : guest , pw : guest 로 로그인 해보았다. 

쿠키 값을 보니 username = 값(guest)로 되어있다.
username = admin 일 때 flag값을 알려주는 것 같다

username 값을 admin으로 바꾸자

flag값을 얻을 수 있었다.