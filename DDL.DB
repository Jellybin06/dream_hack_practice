CREATE DATABASE Dreamhack;

USE Dreamhack;
# Board 이름의 테이블 생성
CREATE TABLE Board(
    idx INT AUTO_INCREMENT,
    boardTitle VARCHAT(100)
NOT NULL,
    boardContent
VARCHAR(2000) NOT NULL,
    PRIMARY KEY(idx)
);